VITE_BACKEND_BASE_URL=http://localhost:3033
# Cloudflare Turnstile site (public) key, from your Cloudflare dashboard.
# Leave empty to disable the bot check (backend must also have an empty TURNSTILE_SECRET_KEY).
VITE_TURNSTILE_SITE_KEY=
